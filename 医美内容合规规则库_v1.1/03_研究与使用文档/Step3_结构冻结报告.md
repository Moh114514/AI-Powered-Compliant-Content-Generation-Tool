# Step 3 结构冻结报告 —— 医美内容合规规则库 v1.1

> 生成时间：2026-07-27
> 阶段目标：在保留 v1.0 全部数据结构、ID 体系、目录体系的前提下，建立 v1.1 工作副本并冻结结构

---

## 1. 目录复制与 v1.0 保护

- 已执行 `cp -r 医美内容合规规则库_v1.0 医美内容合规规则库_v1.1`，生成独立工作副本。
- **v1.0 原始目录未做任何改动**（已验证 `医美内容合规规则库_v1.0/01_合规规则库主表/` 仍含两份原始 Excel）。
- v1.1 后续所有修改仅作用于 `医美内容合规规则库_v1.1/`，与 v1.0 物理隔离。

## 2. 权威数据源确认

- **JSON（`02_系统调用数据/*.json`）被确立为唯一权威数据源**。
- Excel 不再作为权威；v1.1 的新 Excel 将在 Step 8 由 JSON 重建（旧的 `_build.py` 内联数据为 v1.0 快照，仅作参考，不再作为权威入口）。
- 数据流方向固定为：`JSON（权威）→ CSV（同步）→ Excel（重建）`。

## 3. ID 体系与重编号约束

- 全部现有 ID **保留、不重编号**：
  - 来源 `S0001`–`S0014`（14 条）
  - 核心规则 `R-A01-001`–`R-A16-001`（97 条，含 a/b 子规则）
  - 表达变体 `V000001`–`V000234`（234 条）
  - 规则示例 `E000001`–`E000016`（16 条）
  - 语义规则 `SR0001`–`SR0005`（5 条）
  - 测试样本 `T0001`–`T0200`（200 条）
- 新增数据一律使用**新 ID**（如新增来源 `S0015` 起、案例 `C0001` 起、案例线索 `CL0004` 起），不与旧 ID 冲突。

## 4. 旧 Excel 归档

- `医美内容合规规则库_v1.0.xlsx` → `06_版本管理/历史版本/`
- `医美内容合规规则库_v1.0_batch2.xlsx` → `06_版本管理/历史版本/`
- 归档后 `01_合规规则库主表/` 在 v1.1 中暂无 Excel（待 Step 8 由 JSON 重建新 Excel）。

## 5. 元数据与版本变更

- `metadata.json`：`version` 由 `v1.0` 更新为 `v1.1-dev`；新增 `previous_version`、`authoritative_source` 字段；`review_status` 改为"v1.1 开发中"。
- `changelog.json`：新增两条记录
  - `CHG0004`：version_bump（v1.0 → v1.1-dev）
  - `CHG0005`：structure_freeze（复制副本/JSON 权威/Excel 归档/ID 保留）

## 6. 结构冻结检查结果

| 检查项 | 结果 |
|---|---|
| v1.0 原始目录未被改动 | ✅ 两份 Excel 仍在原处 |
| v1.1 工作副本建立 | ✅ 45→45 文件（含复制的审查报告） |
| 模块完整性（16 个数据模块） | ✅ 全部存在且数量与 v1.0 一致 |
| rule_id 唯一性 / 格式 | ✅ 97 条，dup=0，bad_format=0 |
| source_id 唯一性 / 格式 | ✅ 14 条，dup=0，bad_format=0 |
| 全量 JSON U+FFFD 乱码 | ✅ 0 |
| metadata.version | ✅ v1.1-dev |
| 旧 Excel 归档 | ✅ 已移至 06_版本管理/历史版本 |
| JSON 权威声明 | ✅ metadata.authoritative_source 已写入 |

## 7. 冻结结论

结构已冻结，可安全进入 Step 4（来源库与案例库扩充）。本阶段**未新增任何规则、变体或语义规则**，仅完成副本建立、归档、元数据与变更记录更新。所有扩展数据（Step 4–Step 7）将在不破坏本冻结结构的前提下，以新增 ID 方式追加。

---
*本报告的生成不修改任何 v1.0 文件；v1.1 内所有改动均可经 `git`/文件比对回溯至 v1.0。*
