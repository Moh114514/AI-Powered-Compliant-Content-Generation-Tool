# 默认使用 MockProvider，无需密钥即可运行演示。
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=1200

# 多个来源用逗号分隔。生产环境请改为实际前端地址。
CORS_ORIGINS=http://localhost:5174,http://127.0.0.1:5174
