# 大模型 API 配置（仅需配置 Provider 时使用，留空则自动启用演示模式）

# OpenAI 兼容服务（支持 OpenAI / DeepSeek / 通义 等）
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# 温度与最大 Token（可选，后端有默认值）
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=1200
